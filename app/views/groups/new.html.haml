- provide(:title, "New Group")
.row
  .col.py-3
    = simple_form_for(@group) do |f|
      = f.error_notification
      .form-inputs
        = f.input :name, required: true
        = f.input :icon, as: :file
      .form-actions
        = f.button :submit, "Save", class: 'btn btn-primary btn-lg w-100'
